import './WrongPage.scss';

import React from 'react';

function WrongPage() {
	return (
		<main className='wrongPage'>
			<div className="wrapper">
				<h2>404...</h2>
				<p>The address is entered incorrectly,<br />or the page no longer exists on the site.</p>
			</div>
		</main>
	);
}

export default WrongPage;
